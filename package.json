{
  "private": true,
  "scripts": {
    "start": "export $(cat .env | xargs) && node index.js"
  },
  "dependencies": {
    "@influxdata/influxdb-client": "^1.13.0",
    "puppeteer": "^9.0.0",
    "yargs": "^17.0.0"
  }
}
